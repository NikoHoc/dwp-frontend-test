# DWP Frontend Test - NikoHoc

## How to Run
1. **Install Dependencies**
   ```
   npm install
   ```
2. **Run the app**
    ```
    npm run dev
    ```
3. **Run the server (json-server)**
    <br> Open another terminal then:
    ```
    npm run server
    ```